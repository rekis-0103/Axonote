# Unpinned lower bounds so prebuilt wheels are selected on the local Python
# (Python 3.14 needs recent pydantic-core / no source build). Pin exact versions
# once the team standardizes a Python version (Docker uses 3.12).
fastapi>=0.115
uvicorn>=0.49.0
pydantic>=2.13.4
pydantic-settings>=2.14.1
python-dotenv>=1.2.2
sqlalchemy>=2.0
pymysql>=1.1
python-multipart>=0.0.20
google-auth>=2.34
requests>=2.31
argon2-cffi>=23.1

# dev / test
pytest>=8.3
httpx>=0.28.1
ruff>=0.15.16
